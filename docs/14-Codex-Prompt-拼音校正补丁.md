# Codex Prompt · 拼音校正补丁（朝=早晨 等遗漏）

> 上一轮拼音校正（见 `docs/13`、`docs/pinyin-校正报告.md`）已修正大量破读/通假/专名/虚词读音，但**漏掉「朝」表"早晨"的读音**，并有一处「衣」改得不妥。本补丁只做这几处收尾，机制同前：在 `content/raw/{id}-*.txt` 里给对应字加/改手工 `<ruby>字<rp>(</rp><rt>音</rt><rp>)</rp></ruby>`（覆盖优先，不改动原文汉字与标点）。

---

## 一、必改清单（共 6 处）

**「朝」表"早晨"应读 zhāo（现误为 cháo）——5 处：**

| id | 篇 | 句 | 字 | 改为 | 依据 |
|---|---|---|---|---|---|
| 006 | 邹忌讽齐王纳谏 | 朝服衣冠 | 朝 | **zhāo** | 早晨穿戴（与下文动作对应） |
| 013 | 烛之武退秦师 | 朝济而夕设版焉 | 朝 | **zhāo** | 与"夕"对举，早上 |
| 015 | 岳阳楼记 | 朝晖夕阴 | 朝 | **zhāo** | 与"夕"对举，早上 |
| 016 | 醉翁亭记 | 山间之朝暮也 | 朝 | **zhāo** | "朝暮"=早晚 |
| 016 | 醉翁亭记 | 朝而往，暮而归 | 朝 | **zhāo** | 与"暮"对举，早上 |

**「衣」读音回正——1 处：**

| id | 篇 | 句 | 字 | 改为 | 依据 |
|---|---|---|---|---|---|
| 006 | 邹忌讽齐王纳谏 | 朝服衣冠 | 衣 | **yī** | 通行读法为「朝(zhāo)·服(动词穿戴)·衣冠(名词)」，"衣冠"为名词，衣读 yī；上一轮误改为动词 yì，且与相邻"冠(guān)"不一致，故回正 |

> 注意「朝服衣冠」这一句需**同时**改 朝→zhāo、衣→yī（冠保持 guān 名词）。

---

## 二、不要误伤（这些「朝」是"朝廷/朝见"义，保持 cháo）

以下位置**不得改动**：006 入朝见威王、朝廷之臣、于市朝、皆朝于齐、战胜于朝廷；013 之外无；025 日不听朝；042 载宝而朝。（033「以朝夕献善」已正确读 zhāo，保持不动。）

---

## 三、顺带排查同类（晨昏/对举易漏音）

在 50 篇中再扫一遍，确认下列"随文取义"的字读音正确，发现错的按同法加 `<ruby>` 覆盖，拿不准的**不要猜**、登记到报告"存疑待核"：

- **朝** zhāo(早晨) / cháo(朝廷·朝见)——本补丁重点，确保无遗漏。
- **暮 / 夕 / 晓 / 晨** 一般单音，若与"朝"对举出现，核对无误即可。
- **中** zhōng / zhòng(射中、符合)；**冠** guān(名词) / guàn(动词，行冠礼)；**创** chuāng(伤) / chuàng(开创)——按文意确认。

---

## 四、工作流程与验收

1. 按第一节改 006、013、015、016 四个 raw 文件（006 改两处）。
2. 把本次改动**追加**到 `docs/pinyin-校正报告.md`（同格式：`文件：句 — 字 — 旧音->新音 — 依据`），并把第三节的排查结论/存疑一并记入。
3. `npm run build`（build:data + validate-data + validate-curriculum）必须**全绿**（只动 ruby，不应触发 `Original text mismatch`）。
4. 复核：
   ```bash
   node scripts/scan-pinyin.js   # 若已存在；否则用下方一次性命令
   ```
   或直接核对：006/013/015/016 上述「朝」均为 zhāo，006「衣」为 yī，且第二节 cháo 各处未被误改。
5. `git commit`（如 `fix(pinyin): correct 朝=zhāo readings and 衣 in 006`）。

完成后请输出：改了哪些 raw 文件、各处旧音→新音、`npm run build` 结果、以及第三节排查的结论与存疑清单。
