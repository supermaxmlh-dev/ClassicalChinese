# Codex Prompt · 第二阶段（051–095）拼音校正

> 背景：第一阶段（001–050）已做过文言拼音校正，但**第二阶段 45 篇（051–095）完全没做**——`content/raw/051-*.txt` ~ `095-*.txt` 目前 **0 个手工 `<ruby>`**，原文注音全由 `pinyin-pro` 自动生成，在文言**破读/通假/专名异读/文言虚词/朝=早晨**上系统性读错。"显示全部拼音"会把错音直接展示给学生。
>
> 目标：对 `content/raw/` 下 **051–095 共 45 篇** 校正这些读音。机制同 `docs/13`、`docs/14`：在 raw 底本里给**读错的那一处**加手工 `<ruby>` 覆盖（覆盖优先），**不改原文用字**（`assertSameOriginal` 逐字校验）。第一阶段的报告已在 `docs/pinyin-校正报告.md`，本次追加"第二阶段"小节。

---

## 一、机制与约束（同一阶段）

- 修法：把 raw 中读错的字包成 `<ruby>字<rp>(</rp><rt>正确拼音</rt><rp>)</rp></ruby>`，重建即用手工音。
- **只包需要纠正的字**：自动音正确的不动（为/相/见/数/长 等多数已消歧对）。逐处按上下文判断。
- **不增删改任何汉字与标点**，只加 ruby 包裹。拼音用带声调符号形式（`zhǔ`、`fǒu`、`zhuàn`）。

---

## 二、已确认的错误（务必修，并全篇排查同类）

| 篇 | 词/句 | 字 | 现注(错) | 应为 | 类别 |
|---|---|---|---|---|---|
| 066 师说 | 或**不**焉 | 不 | bù | **fǒu**（不=否） | 通假 |
| 066 师说 | 六艺经**传** | 传 | chuán | **zhuàn**（经传，名词） | 破读 |
| 070 前赤壁赋 | 举酒**属**客 / **属**客 | 属 | shǔ | **zhǔ**（劝请、敬酒；"举匏樽以相属"同） | 破读 |
| 070 前赤壁赋 | **酾**酒临江 | 酾 | shāi | **shī** | 异读 |
| 062 冯谖客孟尝君 | 孟尝君不**说** | 说 | shuō | **yuè**（说=悦） | 通假 |

> 以上为抽查所得；请按第三节清单在 45 篇中**全面排查同类**。

---

## 三、逐篇排查清单（取该义时才改，取普通义保持自动音）

**破读/异读**
- 属：劝酒/嘱托/连缀→**zhǔ**（举酒属客、属予作文）；类属→shǔ
- 传：名词"经传/传记"→**zhuàn**；传递→chuán
- 食：喂养→sì；吃→shí　　衣：穿→yì；衣服→yī
- 语：告诉→yù　　雨：降下→yù　　王：称王→wàng
- 重：重叠/再→chóng；分量→zhòng　　数：屡次→shuò/点数→shǔ
- 骑：坐骑/骑兵（名词）→jì　　创：创伤→chuāng；开创→chuàng
- 藉：枕藉→jiè（如出现）　　酾→shī　　樽/尊、蜉蝣、嫠(lí)妇、愀(qiǎo)然、舳舻(zhúlú)、匏(páo)樽 等赤壁赋生僻字按音标注

**通假（读本字）**
- 说→悦 **yuè**；见→现 **xiàn**；知→智 **zhì**；女→汝 **rǔ**
- 邪→耶 **yé**；不→否 **fǒu**；反→返 fǎn；距→拒 jù；内→纳 nà；曾→增/竟(zēng)

**专名/异读**
- 召 **shào**；知氏 **zhì**；郤/卻 xì；万俟、appellations 等按定音

**文言虚词**
- 夫（发语/指示/感叹）→**fú**；邪/与（疑问感叹语气，=耶/欤）→**yé / yú**；不（句末=否）→**fǒu**

**朝＝早晨读 zhāo**（与夕/暮对举时）——本阶段有名篇涉时序描写（秋声赋、赤壁赋、归去来兮辞等），务必排查；朝廷/朝见义仍读 cháo。

> 本阶段名篇多、破读密集，尤其 **066 师说、070 前赤壁赋、074 后赤壁赋、089 归去来兮辞、090 秋声赋、062 冯谖客孟尝君、063 触龙说赵太后** 要逐句过。

---

## 四、不确定时（重要）

文言读音多有争议，**宁可标注待核也不要猜**：能依通行中学教材/《古汉语常用字字典》《王力古汉语字典》确定的直接改；拿不准的**不改动**，登记到报告"存疑待核"交老师复核；**严禁臆造生僻读音**。

---

## 五、产出与流程

1. **分批处理**（一次一周 3–4 篇，051 起）。在 `content/raw/{id}-*.txt` 加 `<ruby>` 覆盖。
2. 在 `docs/pinyin-校正报告.md` **新增"## 第二阶段（051–095）"小节**，逐处记录：`文件：词 — 字 — 旧音→新音 — 依据`；存疑单列。
3. 每批 `npm run build`（build:data + validate-data + validate-curriculum）**全绿**（只动 ruby 不应触发 `Original text mismatch`）。
4. 用下方脚本自查该阶段读音；分批 `git commit`（如 `fix(pinyin): 文言 readings for 066,070,062`）。

**自查脚本**（列出高危字在 051–095 的当前注音与上下文）：
```js
const fs=require("fs"),path=require("path");
const dir=path.resolve(__dirname,"../data/articles");
const watch=["属","传","食","衣","语","雨","王","重","数","骑","创","说","见","知","女","邪","不","召","与","夫","恶","朝","藉","酾","曾"];
for(const f of fs.readdirSync(dir).filter(x=>/^\d{3}\.json$/.test(x))){
  const id=f.slice(0,3); if(id<"051"||id>"095")continue;
  const d=JSON.parse(fs.readFileSync(path.join(dir,f),"utf8"));
  const plain=d.fullTextPlain;
  d.rubyAnnotations.forEach(r=>{ if(watch.includes(r.char)){
    const ctx=plain.slice(Math.max(0,r.pos-3),r.pos+4).replace(/\n/g,"");
    console.log(`${d.id} ${r.char}→${r.pinyin}  …${ctx}…`);
  }});
}
```

---

## 六、验收标准

1. 第二节 5 处错误在对应 JSON 中已变为正确读音。
2. `npm run build` 全绿；原文用字未变（无 `Original text mismatch`）。
3. 自查脚本输出中，051–095 的破读/通假/专名/虚词/朝=早晨 读音正确；存疑项已在 `docs/pinyin-校正报告.md` 第二阶段小节列明。

完成每批请输出：改了哪些 raw、各处旧音→新音、`npm run build` 结果、存疑待核清单。等我确认再继续下一批。
